"scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives",
    "preview": "vite preview",
    "vercel-build": "node scripts/generate-rss.js && node scripts/generate-sitemap.js && vite build"
  },