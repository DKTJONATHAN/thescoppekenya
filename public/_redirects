# SPA fallback is handled in wrangler.jsonc:
# assets.not_found_handling = single-page-application
# Do not add /* /index.html 200 here — Workers rejects it as an infinite loop
# when Pretty URLs strip .html / index.
#
# GSC verification files are served by worker.js (run_worker_first).
