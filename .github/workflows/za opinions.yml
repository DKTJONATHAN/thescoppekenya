name: Jonathan Mwaniki - Opinions Scraper

on:
  schedule:
    # Runs at 10:25 and 18:25 UTC
    # Positioned to avoid peak traffic from other scrapers
    - cron: '25 10,18 * * *'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

env:
  DEFAULT_BRANCH: main
  POSTS_DIR: content/posts
  MEMORY_FILE: .github/memory_jonathan.json

jobs:
  generate-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.DEFAULT_BRANCH }}
          token: ${{ secrets.PERSONAL_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests google-genai beautifulsoup4 lxml playwright python-dateutil
          playwright install chromium

      - name: Generate Article with Gemini
        env:
          GEMINI_WRITE_KEY: ${{ secrets.GEMINI_WRITE_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_API_KEY1: ${{ secrets.GEMINI_API_KEY1 }}
          POSTS_DIR: ${{ env.POSTS_DIR }}
          MEMORY_FILE: ${{ env.MEMORY_FILE }}
        run: |
          python << 'EOF'
          import os, json, datetime, requests, re, time, sys, hashlib, itertools, textwrap
          from dateutil import parser as date_parser
          from google import genai
          from google.genai import types
          from bs4 import BeautifulSoup
          from playwright.sync_api import sync_playwright

          today_str = datetime.datetime.utcnow().strftime("%Y-%m-%d")
          full_date_str = datetime.datetime.utcnow().strftime("%A, %B %d, %Y")
          MODELS_TO_TRY = ["gemini-3-flash-preview", "gemini-2.5-flash"]

          def get_target_urls():
              urls = []
              try:
                  with sync_playwright() as p:
                      browser = p.chromium.launch(headless=True)
                      page = browser.new_page()
                      page.goto('https://www.the-star.co.ke/opinion/', timeout=60000)
                      soup = BeautifulSoup(page.content(), 'html.parser')
                      for a in soup.find_all('a', href=True):
                          href = a.get('href', '')
                          if '/opinion/' in href and len(href) > 35:
                              urls.append(href if href.startswith('http') else 'https://www.the-star.co.ke' + href)
                      browser.close()
              except: pass
              return list(set(urls))

          def scrape_article(url):
              with sync_playwright() as p:
                  browser = p.chromium.launch(headless=True)
                  page = browser.new_page()
                  try:
                      page.goto(url, timeout=45000)
                      soup = BeautifulSoup(page.content(), "html.parser")
                      meta_pub = soup.find('meta', property='article:published_time')
                      if meta_pub:
                          pub_time = date_parser.parse(meta_pub.get('content')).replace(tzinfo=datetime.timezone.utc)
                          if (datetime.datetime.now(datetime.timezone.utc) - pub_time).total_seconds() / 3600 > 12:
                              return None, None, None
                      title = soup.find('title').get_text().replace(' - The Star', '').strip()
                      body = "\n\n".join([p.get_text() for p in soup.find_all('p') if len(p.get_text()) > 40])
                      img = soup.find('meta', property='og:image')['content'] if soup.find('meta', property='og:image') else ""
                      return body, img, title
                  except: return None, None, None
                  finally: browser.close()

          # API Rotation Logic
          raw_keys = [os.environ.get("GEMINI_WRITE_KEY"), os.environ.get("GEMINI_API_KEY"), os.environ.get("GEMINI_API_KEY1")]
          api_keys = [k for k in raw_keys if k and k.strip()]
          key_cycle = itertools.cycle(api_keys)
          current_key = next(key_cycle)
          client = genai.Client(api_key=current_key)

          def gemini_generate(prompt, use_search=False):
              global current_key, client
              for model_id in MODELS_TO_TRY:
                  try:
                      config = types.GenerateContentConfig(tools=[types.Tool(google_search=types.GoogleSearch())]) if use_search else None
                      resp = client.models.generate_content(model=model_id, contents=prompt, config=config)
                      return resp.text.strip()
                  except Exception:
                      current_key = next(key_cycle)
                      client = genai.Client(api_key=current_key)
                      continue
              return None

          links = get_target_urls()
          memory_path = os.environ.get('MEMORY_FILE')
          memory = json.load(open(memory_path)) if os.path.exists(memory_path) else []
          
          for link in links:
              u_hash = hashlib.md5(link.encode()).hexdigest()
              if u_hash in memory: continue
              text, img, title = scrape_article(link)
              
              if text and len(text) > 500:
                  title_prompt = f"JSON ONLY: {{\"final_title\": \"...\"}}. Create a powerful, click-worthy title for this op-ed: {title}. Keywords: {text[:500]}"
                  res_t = gemini_generate(title_prompt)
                  try: final_title = json.loads(re.search(r'\{.*\}', res_t).group())['final_title']
                  except: final_title = title

                  article_prompt = f"Write a powerful Kenyan op-ed as Jonathan Mwaniki. Source: {text[:9000]}. Use H2 for subheadings. Style: Intellectual, authoritative, professional English. Verified freshness for {full_date_str}."
                  final_body = gemini_generate(article_prompt, use_search=True)
                  
                  if final_body:
                      slug = re.sub(r'[^a-z0-9]+', '-', final_title.lower()).strip('-')[:70]
                      with open(os.path.join(os.environ.get("POSTS_DIR"), f"{slug}.md"), "w") as f:
                          f.write(f"---\ntitle: \"{final_title}\"\nslug: \"{slug}\"\nauthor: \"Jonathan Mwaniki\"\nimage: \"{img if img else 'https://images.unsplash.com/photo-1455390582262-044cdead27d8'}\"\ncategory: \"Opinions\"\ndate: \"{today_str}\"\n---\n\n{final_body}")
                      memory.append(u_hash)
                      with open(memory_path, 'w') as f: json.dump(memory[-200:], f)
                      break
          EOF

      - name: Git Safety Pull
        run: |
          git config --global user.name "Jonathan Mwaniki"
          git config --global user.email "bot@zandani.co.ke"
          git add .
          git stash -u
          git fetch origin main
          git pull origin main --rebase
          git stash pop || echo "Nothing to pop"

      - name: Commit and push article
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_author: "Jonathan Mwaniki <bot@zandani.co.ke>"
          commit_message: "content: Thought-provoking Opinion by Jonathan Mwaniki ✍️"
          file_pattern: 'content/posts/*.md .github/memory_jonathan.json'
