name: Mpasho Scraper (Universal Fix)

on:
  schedule:
    - cron: '0 6,12,18 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PERSONAL_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          pip install playwright google-genai beautifulsoup4 lxml requests
          playwright install chromium

      - name: Run Scraper
        env:
          GEMINI_WRITE_KEY: ${{ secrets.GEMINI_WRITE_KEY }}
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
          POSTS_DIR: content/posts
        run: |
          python << 'EOF'
          import os, random, sys, time, requests, re
          from google import genai
          from bs4 import BeautifulSoup
          from playwright.sync_api import sync_playwright
          from datetime import datetime

          # --- CONFIGURATION ---
          # Google News RSS for Mpasho
          RSS_FEED = "https://news.google.com/rss/search?q=site:mpasho.co.ke+when:1d&hl=en-KE&gl=KE&ceid=KE:en"

          def get_links_from_google():
              print(f"📡 Asking Google News for Mpasho links...")
              try:
                  resp = requests.get(RSS_FEED, timeout=10)
                  soup = BeautifulSoup(resp.content, "xml")
                  items = soup.find_all("item")
                  links = [item.link.text for item in items]
                  print(f"✅ Google found {len(links)} articles.")
                  return links
              except Exception as e:
                  print(f"❌ Google RSS Error: {e}")
                  return []

          def scrape_article_universal(url):
              print(f"🕷️ Stealth-Loading: {url}")
              
              with sync_playwright() as p:
                  browser = p.chromium.launch(
                      headless=True,
                      args=["--disable-blink-features=AutomationControlled", "--no-sandbox"]
                  )
                  context = browser.new_context(
                      user_agent="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
                  )
                  page = context.new_page()

                  # Disable navigator.webdriver to hide from bot detection
                  page.add_init_script("Object.defineProperty(navigator, 'webdriver', {get: () => undefined})")

                  try:
                      page.goto(url, timeout=60000)
                      
                      # WAIT STRATEGY: Wait for network to be idle (ads finished loading)
                      try:
                          page.wait_for_load_state("networkidle", timeout=15000)
                      except:
                          print("⚠️ Network didn't settle, but proceeding with DOM...")
                          pass

                      content = page.content()
                      soup = BeautifulSoup(content, "html.parser")
                      
                      # 1. GET TITLE
                      h1 = soup.find("h1")
                      title = h1.text.strip() if h1 else soup.title.string
                      print(f"   Title Found: {title}")

                      # 2. GET TEXT (The Smart Way)
                      # Instead of looking for one specific div, we try a prioritized list
                      # If that fails, we just grab ALL paragraphs > 60 chars.
                      
                      article_text = ""
                      
                      # Strategy A: Semantic Tags
                      potential_containers = soup.find_all(['article', 'main', 'div'], class_=re.compile(r'(body|content|entry|post)'))
                      
                      for container in potential_containers:
                          # Check if this container has enough text
                          paras = container.find_all("p")
                          temp_text = "\n\n".join([p.text.strip() for p in paras if len(p.text) > 50])
                          if len(temp_text) > 500: # Found a good chunk
                              article_text = temp_text
                              print("   ✅ Found text in container.")
                              break
                      
                      # Strategy B: The "Vacuum" (Grab all long paragraphs)
                      if not article_text:
                          print("   ⚠️ Strategy A failed. Using Vacuum Method...")
                          all_paras = soup.find_all("p")
                          # Filter: Must be > 60 chars and not contain 'copyright' or 'cookie'
                          clean_paras = []
                          for p in all_paras:
                              txt = p.text.strip()
                              if len(txt) > 60 and "cookie" not in txt.lower():
                                  clean_paras.append(txt)
                          article_text = "\n\n".join(clean_paras)

                      browser.close()
                      return title, article_text

                  except Exception as e:
                      print(f"❌ Playwright Error: {e}")
                      browser.close()
                      return None, None

          # --- MAIN EXECUTION ---
          candidates = get_links_from_google()
          if not candidates: sys.exit(0)
          
          random.shuffle(candidates)
          
          final_title, final_text = None, None
          
          # Try up to 3 links
          for link in candidates[:3]:
              t, txt = scrape_article_universal(link)
              if txt and len(txt) > 300:
                  final_title, final_text = t, txt
                  break
              print("   Retrying next link...")

          if not final_text:
              print("❌ All scraping attempts failed.")
              sys.exit(1)

          print(f"🎯 Success! Ready to rewrite.")

          # --- REWRITE (Gemini) ---
          print("🤖 Rewriting...")
          client = genai.Client(api_key=os.environ["GEMINI_WRITE_KEY"])
          
          prompt = f"""
          Rewrite this for a Nairobi Gossip Blog (Sheng).
          Title: {final_title}
          Body: {final_text[:6000]}
          
          Format:
          TITLE: [Sheng Title]
          SLUG: [slug]
          EXCERPT: [Hook]
          CATEGORY: Entertainment
          TAGS: Gossip, Nairobi
          IMAGE_KEYWORD: [Search Term]
          BODY:
          [Markdown Text]
          """
          
          try:
              resp = client.models.generate_content(model="gemini-2.0-flash", contents=prompt)
              final_text = resp.text
          except Exception as e:
              print(f"❌ Gemini Error: {e}")
              sys.exit(1)

          # --- SAVE ---
          parsed = { "TITLE": "", "SLUG": "", "EXCERPT": "", "CATEGORY": "", "TAGS": "", "IMAGE_KEYWORD": "", "BODY": "" }
          current_section = None
          for line in final_text.splitlines():
              clean = line.strip().replace("**", "")
              if clean.startswith("TITLE:"): parsed["TITLE"] = clean.replace("TITLE:", "").strip()
              elif clean.startswith("SLUG:"): parsed["SLUG"] = clean.replace("SLUG:", "").strip()
              elif clean.startswith("EXCERPT:"): parsed["EXCERPT"] = clean.replace("EXCERPT:", "").strip()
              elif clean.startswith("CATEGORY:"): parsed["CATEGORY"] = clean.replace("CATEGORY:", "").strip()
              elif clean.startswith("TAGS:"): parsed["TAGS"] = clean.replace("TAGS:", "").strip()
              elif clean.startswith("IMAGE_KEYWORD:"): parsed["IMAGE_KEYWORD"] = clean.replace("IMAGE_KEYWORD:", "").strip()
              elif clean.startswith("BODY:"):
                  current_section = "BODY"
                  continue
              elif current_section == "BODY":
                  parsed["BODY"] += line + "\n"

          # Image
          def get_image(query):
              key = os.environ.get("UNSPLASH_ACCESS_KEY")
              fallback = "https://images.unsplash.com/photo-1499364615650-ec387c1470c5?w=1200"
              if not key: return fallback
              try:
                  u = f"https://api.unsplash.com/photos/random?query={query}&orientation=landscape&client_id={key}"
                  r = requests.get(u, timeout=10)
                  return r.json()['urls']['regular'] if r.status_code == 200 else fallback
              except: return fallback

          image_url = get_image(parsed['IMAGE_KEYWORD'] or "Kenya")
          today_str = datetime.utcnow().strftime("%Y-%m-%d")

          md_content = f"""---
          title: "{parsed['TITLE'].replace('"', "'")}"
          slug: "{parsed['SLUG']}"
          excerpt: "{parsed['EXCERPT'].replace('"', "'")}"
          image: "{image_url}"
          category: "{parsed['CATEGORY']}"
          date: "{today_str}"
          tags: [{parsed['TAGS']}]
          ---

          {parsed['BODY'].strip()}
          """

          out_dir = os.path.join(os.getcwd(), os.environ.get("POSTS_DIR", "content/posts"))
          os.makedirs(out_dir, exist_ok=True)
          filename = f"{parsed['SLUG']}.md"
          
          with open(os.path.join(out_dir, filename), "w", encoding="utf-8") as f:
              f.write(md_content)
          print(f"✅ Published: {filename}")
          EOF

      - name: Commit and push article
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "content: mpasho universal scoop 🕵️"
          branch: main
          file_pattern: content/posts/*.md