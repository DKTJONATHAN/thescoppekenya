name: DISABLED | Ghost Ghafla (use za-ghafla.yml)

# This file only exists so the old workflow registration (id 242364307)
# stops showing the bare path in Actions and can never run again.
# Live Ghafla pipeline is: .github/workflows/za-ghafla.yml

on:
  workflow_dispatch: {}

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Ghost workflow permanently disabled. Use za-ghafla.yml."
