name: Zed Mogaka - George Diano Stories
on:
  schedule:
    - cron: '0 6,18 * * *'
  workflow_dispatch: {}
concurrency:
  group: ${ { github.workflow }}-${ { github.ref }}
  cancel-in-progress: false
permissions:
  contents: write
env:
  DEFAULT_BRANCH: main
  POSTS_DIR: content/posts
  MEMORY_FILE: .github/memory_georgediano.json
jobs:
  scrape-and-publish:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.DEFAULT_BRANCH }}
          token: ${{ secrets.PERSONAL_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
          persist-credentials: true

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip
          pip install requests google-genai beautifulsoup4 lxml python-dateutil Pillow

      - name: Scrape Syndication and publish article
        env:
          GEMINI_WRITE_KEY: ${{ secrets.GEMINI_WRITE_KEY }}
          GEMINI_API_KEY: $tû secrets.GEMINI_API_KEY }}
          GEMINI_API_KEY11 ${ { secrets.GEMINI_API_KEY11 }}
          IMGBB_API_KEY: ${{ secrets.IMGBB_API_KEY }}
          POSTS_DIR: ${ { env.POSTS_DIR }}
          MEMORY_FILE: ${ { env.MEMORY_FILE }}
        run: |
          python << 'EOF'
          import os, json, datetime, requests, re, time, sys, hashlib, itertools, base64, random, io
          from dateutil import parser as date_parser
          from google import genai
          from google.genai import types
          from bs4 import BeautifulSoup
          from PIL import Image

          publish_timestamp = datetime.datetime.utcnow().strftime("%Y-%m-%dTH%:%M:%SZ")
          today_str         = datetime.datetime.utcnow().strftime("%Y-%m-%d")
          full_date_str     = datetime.datetime.utcnow().strftime("%A, %B %d, %Y")
          current_year      = datetime.datetime.utcnow().strftime("%Y")
          SITE_BASE_URL     = "https://zandani.co.ke"
          MODELS_TO_TRY     = ["gemini-2.5-flash", "gemini-2.0-flash"]
          MAX_AGE_HOURS     = 24
          TARGET_ACCOUNT    = "georgediano"
          MIN_CHARS         = 500

          DIANO_CONTEXT = """
          ABOUT @georgediano (George T. Diano):
          He is a popular Kenyan satirist, storyteller, and social media commentator.
          His bio says: "for Satire & Peasants || Story teller || Inspired by Wahome Mutah."
          He writes in a witty, sarcastic, sharp style mixing English, Sheng, and Kenyan slang.
          He covers Kenyan politics, celebrity gossip, social commentary, and trending stories.
          His posts are often satirical - he says the opposite of what he means to make a point.

          CRITICAL RULE: When his post is SATIRE, do NOT report it as fact.
          Instead, capture the satirical observation or irony and write the story around the underlying truth he is pointing at.

          SHENG & KENYAN SLANG GLOSSARY (translate these correctly):
          - "Buana" = "Man" / "Dude" / "Honestly" (expression of disbelief or emphasis)
          - "Hoi polloi" = ordinary Kenyans / the masses (he uses this ironically)
          - "Nikama" = "It is like" / "It seems like"
          - "Peasants" = his affectionate/ironic name for his followers (not an insult)
          - "Watu wetu" = "Our people" (ironic when pointing out hypocrisy in a group)
          - "Kuosha" = to expose / to humiliate someone publicly
          - "Kulipa fare" = to face consequences / to pay the price
          - "Kutoka kwa" = coming from / originating from
          - "Goon" = thug / political enforcer / hired muscle
          - "Commander" / "General" = sarcastic praise often directed at politicians he is mocking
          - "Deep. Very deep." = sarcastic response to something absurd or obvious
          - "From the pocket straight to the pan" = going from bad to worse / reckless behavior
          - "Eating depression raw" = suffering silently without help
          - "That other side of ODM" = the militant/violent wing of a political party
          - "Eliminated" = killed / assassinated (used seriously in political context)
          - "True General" = sarcastic title, often mockery of a politician acting tough
          - Short cryptic posts often reference a trending Kenyan news story - use Google Search to find the story he is referring to
          """

          BANNED_PHRASES = [
              "sasa basi", "melting the pot", "spill the tea", "tea is hot",
              "grab your popcorn", "buckle up", "breaking news", "dive in",
              "delve into", "moreover", "furthermore", "in conclusion",
              "it's worth noting", "a testament to", "navigating the landscape",
              "in today's digital age", "tapestry", "it is worth noting"
          ]

          def upload_to_imgbb(image_url):
              api_key = os.environ.get("IMGBB_API_KEY")
              if not api_key or not image_url:
                  return image_url
              try:
                  img_data = requests.get(image_url, timeout=15).content
                  img = Image.open(io.BytesIO(img_data))
                  if img.mode in ('RGBA', 'LA', 'P'):
                      img = img.convert('RGB')
                  webp_buf = io.BytesIO()
                  img.save(webp_buf, format='WEBP', quality=82)
                  webp_buf.seek(0)
                  b64 = base64.b64encode(webp_buf.read()).decode('utf-8')
                  res = requests.post(
                      "https://api.imgbb.com/1/upload",
                      data={"key": api_key, "image": b64},
                      timeout=20
                  )
                  if res.status_code == 200:
                      return res.json()['data']['url']
                  return image_url
              except Exception as e:
                  print(f"ImgBB/WebP error: {e}")
                  return image_url

          def get_diano_tweets():
              target_url = f&¢https://syndication.twitter.com/srv/timeline-profile/screen-name/{TARGET_ACCOUNT}"
              tweets = []
              print(f"Scraping syndication feed: {target_url}")
              try:
                  headers = {
                      'Accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,)poriglimage/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7',
                      $Accept-Language': 'en-US,en;q=0.9',
                      'Connection': 'keep-alive',
                      'Upgrade-Insecure-Requests': '1',
                      $User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/134.0.0.0 Safari/537.36'
                  }
                  req = requests.get(target_url, headers=headers, timeout=30)
                  req.raise_for_status()
                  
                  html_content = req.text
                  soup = BeautifulSoup(html_content, "html.parser")

                  data_div = soup.find('div', id='__NEXT_DATA__')
                  if not data_div:
                      print("Could not find NEXT_DATA. Might be rate limited or format changed.")
                      return tweets

                  j_data = json.loads(data_div.string)
                  timeline_data = j_data.get('props', {}).get('pageProps', {}).get('timeline', {})
                  items = timeline_data.get('entries', [])
                  
                  print(f'Found {len(items)} posts via Syndication API')

                  now_utc = datetime.datetime.now(datetime.timezone.utc)
                  
                  for item in items:
                      tweet_obj = item.get('tweet', {})
                      if not tweet_obj:
                          continue

                      # Skip retweets
                      if tweet_obj.get('retweeted_status_id_str'):
                          continue

                      text = tweet_obj.get('text', "")
                      if len(text) < MIN_CHARS:
                          continue

                      tweet_id = tweet_obj.get('id_str') or hashlib.md5(text.encode()).hexdigest()
                      tweet_url = fbhttps://x.com/{TARGET_ACCOUNT}/status/{tweet_id}"
                      
                      date_str = tweet_obj.get('created_at')
                      pub_dt = None
                      if date_str:
                          try:
                              pub_dt = date_parser.parse(date_str)
                              if pub_dt.tzinfo is None:
                                  pub_dt = pub_dt.replace(tzinfo=datetime.timezone.utc)
                          exception:
                              pub_dt = None

                      if pub_dt:
                          age_hours = (now_utc - pub_dt).total_seconds() / 3600
                          if age_hours > MAX_AGE_HOURS:
                              continue

                      img_url = ""
                      media = tweet_obj.get('photos', [])
                      if media and len(media) > 0:
                          img_url = media[0].get('url', "")

                      tweets.append({
                          "id": tweet_id,
                          "text": text,
                          "url": tweet_url,
                          "published": pub_dt.isoformat() if pub_dt else "",
                          "image": img_url,
                      })
                      print(f"Accepted post: {len(text)} chars")
                  
              except Exception as e:
                  print(f"Syndication error: {e}")
              return tweets

          raw_keys = [os.environ.get(k) for k in ["GEMINI_WRITE_KEY", "GEMINI_API_KEY", "GEMINI_API_KEY11"] if os.environ.get(k)]
          if not raw_keys:
              print("No Gemini keys found")
              sys.exit(1)

          key_cycle = itertools.cycle(raw_keys)
          current_key = next(key_cycle)
          client = genai.Client(api_key=current_key)

          def gemini_generate(prompt, label="", use_search=False):
              global current_key, client
              for model in MODELS_TO_TRY:
                  for _ in range(6):
                      try:
                          config = types.GenerateContentConfig(
                              tools=[types.Tool(google_search=types.GoogleSearch())]
                          ) if use_search else None
                          resp = client.models.generate_content(model=model, contents=prompt, config=config)
                          candidate = resp.candidates[0] if resp.candidates else None
                          if candidate and candidate.content and candidate.content.parts:
                              result = "".join(p.text for p in candidate.content.parts if hasattr(p, "text") and p.text).strip()
                          else:
                              result = (resp.text or "").strip()
                          print(f"Gemini OK {model}): {label}")
                          return result
                      exception Exception as e:
                          if "429" in str(e) or "quota" in str(e).lower() or "rate" in str(e).lower():
                              time.sleep(random.uniform(20, 40))
                              current_key = next(key_cycle)
                              client = genai.Client(api_key=current_key)
                              continue
                          print(f"Gemini error ({model}): {e}")
                          break
              return None

          def get_internal_links():
              posts_dir = os.environ.get("POSTS_DIR", "content/posts")
              if not os.path.exists(posts_dir):
                  return ""
              files = [f for f in os.listdir(posts_dir) if f.endswith('.md')]
              sample = random.sample(files, min(len(files), 3))
              links = []
              for fn in sample:
                  try:
                      with open(os.path.join(posts_dir, fn), 'r', encoding='utf-8') as f:
                          content = f.read()
                      title_m = re.search(r'title:\ **"(.*?)"', content)
                      slug_m = re.search(r'slug:\ ***"(.*?)"', content)
                      if title_m and slug_m:
                          links.append(f'- [{title_m.group(1)}]({SITE_BASE_URL}/article/{slug_m.group(1)})')
                  exception Exception:
                      pass
              return "\n".join(links if links else ""

          memory_path = os.environ.get("MEMORY_FILE", ".github/memory_georgediano.json")
          memory = json.load(open(memory_path)) if os.path.exists(memory_path) else []

          tweets = get_diano_tweets()
          if not tweets:
              print("No qualifying tweets found.")
              sys.exit(0)

          chosen = None
          for tweet in tweets:
              if tweet["id"] not in memory:
                  chosen = tweet
                  break

          if not chosen:
              print("All qualifying tweets already published.")
              sys.exit(0)

          interpret_prompt = (
              DIANO_CONTEXT + "\n\n"
              + "DIANO'S POST:\n" + chosen["text"] + "\n\n"
              + "TASK: This post is heavily stylistic. Before writing an article, analyze it.\n"
              + "1. Is this tweet SATIRICAL, SLANG-HEAVY, or a STRAIGHT OBSERVATION? (answer: satirical / slang / literal)\n"
              + "2. Search Google for the ACTUAL TRENDING KENYAN STORY he is referencing.\n"
              + "3. What is the true event? (Summarize it plainly)\n"
              + "4. What is Diano's core commentary or joke about this event?\n"
              + "Output ONLY JSON: {\n"
              + '"tone_identified": "satirical|slang|literal",\n'
              + '"true_story": "The actual trending event",\n'
              + '"diano_commentary": "What he is pointing out"\n'
              + "}"
          )
          interpret_text = gemini_generate(interpret_prompt, "interpret", use_search=True)
          interpretation = {}
          if interpret_text:
              try:
                  clean = re.sub(r'^``bjson\n"|```$', '', interpret_text, flags=re.I | re.DOTALL).strip()
                  interpretation = json.loads(clean)
              exception Exception as e:
                  print(f"Interpret parse error: {e}")

          current_key = next(key_cycle)
          client = genai.Client(api_key=current_key)
          time.sleep(5)

          true_story = interpretation.get("true_story", chosen["text"][:500])
          commentary = interpretation.get("diano_commentary", "")

          title_prompt = (
              "TRUE TRENDING STORY: " + true_story + "\n"
              + "POST COMMENTARY: " + commentary + "\n\n"
              + "Write a single punchy headline for a Kenyan gossip/entertainment article about this trending story.\n"
              + "Style: bold, intriguing, Kenyan pop culture tone. No clickbait questions. No brackets.\n"
              + "The headline must reflect the REAL story. DO NOT mention George Diano or Twitter/X in the headline.\n"
              + 'Output ONLY JSON: {"final_title": "Your Headline Here"}'
          )
          title_text = gemini_generate(title_prompt, "title")
          final_title = chosen["text"][:80].split("\n")[0]
          if title_text:
              try:
                  clean = re.sub(r'eh``json\n?|```$', '', title_text, flags=re.I | re.DOTALL).strip()
                  final_title = json.loads(clean).get("final_title", final_title)
              exception Exception:
                  pass

          current_key = next(key_cycle)
          client = genai.Client(api_key=current_key)
          time.sleep(5)

          internal_links = get_internal_links()
          banned_str = ", ".join(BANNED_PHRASES)

          article_prompt = (
              "Current Date: " + full_date_str + ". Current year: " + current_year + ".\n\n"
              + DIANO_CONTEXT + "\n\n"
              + "TRENDING STORY TO COVER: " + true_story + "\n"
              + "INSIDER COMMENTARY TO INCLUDE: " + commentary + "\n"
              + "ARTICLE TITLE: " + final_title + "\n"
              + "INTERNAL LINKS (weave in 1-2 naturally):\n" + internal_links + "\n\n"
              + "TASK: Write a full Kenyan entertainment/gossip article for Za Ndani.\n"
              + "Author is Zed Mogaka - his voice is witty, sharp, and feels like aninsider dropping the real gist.\n"
              + "The article should tell the REAL story based on the trending topic.\n"
              + "Write in third-person news style. No self-referencing as the author.\n"
              + "Translate any Sheng/slang into clear vivid English so all readers understand.\n"
              + "No em-dashes. Single hyphens only. No title/byline/date in body.\n"
              + "All date references must be " + current_year + ".\n\n"
              + "STRUCTURE:\n"
              + "1. A 35-45 word lede paragraph (no heading above it) - answers who/what/when/where immediately.\n"
              + "2. ## H2 subheading with primary keyword.\n"
              + "3. 3-4 supporting paragraphs with ### subheadings.\n"
              + "4. A sharp closing line that captures the Kenyan street reaction.\n\n"
              + "BANNED PHRASES (never use): " + banned_str + "\n"
              + "CRITICAL: NEVER mention George Diano as the source. NEVER mention Twitter/X/social media.\n"
              + "Write as if Zed Mogaka and Za Ndani gathered this story directly.\n"
              + "Do NOT attribute quotes to Diano by name.\n"
              + "Make it entertaining, vivid, and unmistakably Kenyan in flavour."
          )
          article_text = gemini_generate(article_prompt, "article", use_search=True)
          if not article_text:
              sys.exit(1)

          article_text = re.sub(r'eh``(?:markdown)?\n?', '', article_text).rstrip('```').strip()
          article_text = re.sub(r'^(Title|By|Author|Date):.*?\n', '', article_text, flags=re.I | re.M)
          article_text = article_text.replace('\u2014', '-').replace('\u2013', '-')

          current_key = next(key_cycle)
          client = genai.Client(api_key=current_key)
          time.sleep(3)

          desc_prompt = (
              "Write a single meta description (140-155 characters) for this article.\n"
              + "Title: " + final_title + "\n"
              + "Snippet: " + article_text[:600] + "\n"
              + "Rules: factual, no clickbait, starts with subject or verb, includes key news fact.\n"
              + 'Output ONLY JSON: {"description": "Your description here"}'
          )
          desc_text = gemini_generate(desc_prompt, "description")
          meta_description = ""
          if desc_text:
              try:
                  clean = re.sub(r'^``bjson\n"|```$', '', desc_text, flags=re.I | re.DOTALL).strip()
                  meta_description = json.loads(clean).get("description", "")[:160].replace('"', '\'')
              exception Exception:
                  pass

          time.sleep(3)

          tags_prompt = (
              "Generate 4-5 relevant SEO tags for this Kenyan entertainment article.\n"
              + "Title: " + final_title + "\n"
              + "Rules: lowercase, hyphenated multi-word. No generic tags like 'news', 'kenya'.\n"
              + 'Output ONLY JSON: {"tags": ["tag-one", "tag-two", "tag-three"]}'
          )
          tags_text = gemini_generate(tags_prompt, "tags")
          article_tags = []
          if tags_text:
              try:
                  clean = re.sub(r'^```json\n?|```"', '', tags_text, flags=re.I | re.DOTALL).strip()
                  article_tags = [str(t).lower().replace(' ', '-') for t in json.loads(clean).get("tags", [])[:5]]
              exception Exception:
                  pass

          final_image = upload_to_imgbb(chosen["image"]) if chosen.get("image") else ""
          slug = re.sub(r'[^a-z0-9]+', '-', final_title.lower()).strip('-')[:68]
          canonical = fi{SITE_BASE_URL}/article/{slug}"
          author_url = fi{SITE_BASE_URL}/author/zed-mogaka"
          tags_json = json.dumps(article_tags)
          posts_dir = os.environ.get("POSTS_DIR", "content/posts")
          os.makedirs(posts_dir, exist_ok=True)

          lines = [
              "---",
              'title: "' + final_title.replace('"', \l'") + '"',
              'slug: "' + slug + '"',
              'description: "' + meta_description + '"',
              'author: "Zed Mogaka"',
              'author_url: "' + author_url + '"',
              'image: "' + final_image + '"',
              'category: "Entertainment"',
              'tags: ' + tags_json,
              'canonical: "' + canonical + '"',
              'date: "' + publish_timestamp + '"',
              "---",
              article_text,
              ""
          ]
          filepath = os.path.join(posts_dir, today_str + '-' + slug + '.md')
          with open(filepath, 'w', encoding='utf-8') as f:
              f.write('\n'.join(lines))

          memory.append(chosen["id"])
          os.makedirs(os.path.dirname(memory_path), exist_ok=True)
          with open(memory_path, 'w') as f:
              json.dump(memory[-500:], f)

          print(f"Saved: {filepath}")
          EOF

      - name: Git Safety Pull
        run: |
          git config --global user.name "Zed Mogaka"
          git config --global user.email "bot@zandani.co.ke"
          git add .
          git stash -u
          git fetch origin main
          git pull origin main --rebase
          git stash pop || echo "Nothing to pop"

      - name: Commit and push
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_author: "Zed Mogaka <bot@zandani.co.ke>"
          commit_message: "content: George Diano story by Zed Mogaka"
          branch: ${{ env.DEFAULT_BRANCH }}
          file_pattern: "content/posts/*.md .github/memory_georgediano.json"

      - name: Ping Google Sitemap
        run: |
          curl -s "https://www.google.com/ping?sitemap=https://zandani.co.ke/sitemap.xml" || true
          echo "Google sitemap pinged"
